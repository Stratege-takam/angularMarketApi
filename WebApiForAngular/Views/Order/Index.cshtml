@model IEnumerable<WebApiForAngular.Models.Order>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OrderNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CustemerId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PMethod)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GTotal)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OrderNo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CustemerId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PMethod)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GTotal)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
